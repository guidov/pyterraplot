# Upper bounds are deliberate: RTD resolves these fresh on every build, so an
# unpinned range would silently adopt a breaking major and break the docs with
# no repo change.
sphinx>=7.2,<9
furo>=2024.1.29
myst-parser>=2.0,<5
sphinx-copybutton>=0.5,<1
linkify-it-py>=2.0,<3
